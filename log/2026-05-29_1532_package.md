# /package

## 사용 프롬프트
`/package`

## 주요 결정

- **클린 빌드로 재현성 검증**: `rm -rf build` 후 cmake 구성→빌드→ctest 전체 재실행. 빌드 경고 0, 53/53 테스트 통과 확인.
- **통합 diff 매트릭스 신설**: 각 단계 로그의 `## 차이 표기`를 `docs/maintenance/diff-matrix.md` 한 곳에 통합. 변경 16개 핵심 파일(+SA 3 재생성)과 **일치 파일(인터페이스 6·stub 6·sim 6·비센서 구현·CMakeLists 등)을 빠짐없이 명시** — "일치"가 무엇인지 보이는 것이 채점 핵심이라는 정책 반영. 기계적 근거(`diff -rq`)도 §4에 첨부.
- **발표 인덱스 신설**: `log/INDEX.md`에 9단계 로그를 실행 순서대로 링크하고 각 단계의 변경/일치를 1줄 요약. 발표자가 "무엇이 바뀌고 무엇이 그대로인지"를 한눈에 보도록 구성.
- **README 최종화**: §3에 최종 빌드·테스트 결과 표(클린 빌드 경고 0, ctest 53/53, 단위 29·시스템 24, SA 클린) 추가. 시스템 테스트의 ctest 필터(`-R 'UC0|SystemScenario'`) 주의 명시. §5에 `log/INDEX.md` 링크 추가.
- **의존성 무변경 확인**: README apt 목록(build-essential·cmake·ninja-build·libgtest-dev·cppcheck·clang-tidy·lcov)이 legacy와 동일 — 새 외부 의존성 미도입.
- **CMakeLists 완전 일치 확인**: `diff -q legacy/CMakeLists.txt CMakeLists.txt` 출력 없음 → 빌드 스크립트는 손대지 않음(소스 glob 방식이라 우측 센서 삭제가 강제하지 않음).

## 생성·수정 파일

- (신규) `C:\Users\윤성진\OneDrive\바탕 화면\Konkuk\3-1\객체지향개발방법론\과제\TeamPractice#6\#5_Team7_NoRight\docs\maintenance\diff-matrix.md`
- (신규) `C:\Users\윤성진\OneDrive\바탕 화면\Konkuk\3-1\객체지향개발방법론\과제\TeamPractice#6\#5_Team7_NoRight\log\INDEX.md`
- (수정) `C:\Users\윤성진\OneDrive\바탕 화면\Konkuk\3-1\객체지향개발방법론\과제\TeamPractice#6\#5_Team7_NoRight\README.md` — 최종 빌드/테스트 결과 표 + INDEX 링크
- (로그) `C:\Users\윤성진\OneDrive\바탕 화면\Konkuk\3-1\객체지향개발방법론\과제\TeamPractice#6\#5_Team7_NoRight\log\2026-05-29_1532_package.md`

## 차이 표기 — 전 아티팩트 통합 집계

기계적 근거: `diff -rq legacy/<dir> <dir>` (상세는 `docs/maintenance/diff-matrix.md`).

| 아티팩트군 | 변경(MODIFY) | 일치(KEEP) |
|---|---|---|
| 문서 docs/ | SRS 1, SDD 1, specs 3, SA 3(재생성) = **8** | — |
| 코드 include/ | types, cleaning_engine.hpp, obstacle_handler.hpp = **3** | 인터페이스 6, clean_intensity_controller.hpp, app 헤더 2 |
| 코드 src/ | main, cleaning_engine.cpp, obstacle_handler.cpp = **3** | clean_intensity_controller.cpp, console_log.cpp, cli_command_source.cpp |
| 빌드 | — | **CMakeLists.txt (완전 일치, diff 없음)** |
| 테스트 simulator/ | rvc_sim_harness.hpp = **1** | sim_proximity_sensor + 5 sim |
| 테스트 unit/ | test_obstacle_handler, test_cleaning_engine = **2** | test_clean_intensity_controller, stub_proximity_sensor + 5 stub |
| 테스트 system/ | test_system_scenarios, test_uc_scenarios = **2** | — |
| **합계** | **변경 19 항목 (핵심 16 + SA 재생성 3)** | **일치 다수 (인터페이스 6·stub 6·sim 6·비센서 구현/헤더 5·CMakeLists 1 등 약 24+)** |
| 신규 | impact.md, diff-matrix.md = **2** | — |

- `Only in legacy/...: .gitkeep` 항목은 placeholder로 산출물 차이 아님.

## 결과 요약

- **클린 빌드**(rm -rf build → cmake → build) 성공, **컴파일 경고 0**.
- **전체 테스트 53/53 통과(100%)** — 단위 29 + 시스템 24. 총 테스트 시간 ≈ 2.1초.
- **정적 분석** error/warning/style/performance/portability 전부 0 (legacy 일치).
- legacy 대비 집계: **변경 핵심 16파일**(+SA 3 재생성, +신규 2) / **일치 다수**(인터페이스·stub·sim·비센서 컴포넌트·CMakeLists). 회피 로직만 "좌 가능→좌회전/좌 막힘→후진"으로 단순화, 우회전 모터 명령은 존속.
- 조교 재현: 한 줄 빌드/테스트 명령 + `legacy/` 대비 `diff -rq` 비교 + 통합 diff 매트릭스 완비.

## 검증 방법

```bash
# 클린 빌드 + 전체 테스트 (한 줄)
wsl bash -lc "cd '/mnt/c/Users/윤성진/OneDrive/바탕 화면/Konkuk/3-1/객체지향개발방법론/과제/TeamPractice#6/#5_Team7_NoRight' && rm -rf build && cmake -S . -B build && cmake --build build -j && ctest --test-dir build --output-on-failure"

# legacy 대비 변경/일치 확인
wsl bash -lc "cd '<repo>' && diff -rq legacy/docs docs; diff -rq legacy/include include; diff -rq legacy/src src; diff -rq legacy/test test; diff -q legacy/CMakeLists.txt CMakeLists.txt"
```

기대: 빌드 경고 0, `100% tests passed ... out of 53`. CMakeLists는 diff 출력 없음(일치).

## 다음 단계

제출. (모든 슬래시 커맨드 완료: `/change-impact`→`/srs`→`/sdd`→`/code`→`/ut`→`/simulator`→`/st`→`/sa`→`/package`)
