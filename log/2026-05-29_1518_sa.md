# /sa

## 사용 프롬프트
`/sa`

## 주요 결정

- **방법론을 legacy와 동일하게 유지**: cppcheck·clang-tidy 바이너리가 WSL 환경에 설치되어 있지 않음(legacy 분석 시점과 동일). legacy SA가 GCC `-Wall -Wextra -Wpedantic -Wconversion -Wshadow`(cppcheck 대체) 및 GCC `-fanalyzer`(clang-tidy 대체)를 사용했으므로, **비교의 등가성을 위해 동일한 GCC 명령**을 수정 코드에 재실행. 새 의존성 미도입(CLAUDE.md §3).
- **수정 코드 기준 재분석**: 최상위 `src/`·`include/`(legacy 아님) 6개 .cpp를 대상으로 진단.
- **legacy 대비 등가 비교 보장**: legacy의 -fanalyzer 5건(STL 거짓 양성)이 본 재실행에서 0건이 된 원인을 규명하기 위해, **동일 명령을 legacy/src 에도 재실행** → legacy도 0건임을 확인. 따라서 5→0은 코드 개선이 아니라 toolchain 차이(GCC Bug #107417 거짓 양성의 현재 toolchain 미재현)임을 summary에 명시.
- **억제 판단**: portability POSIX 헤더(`cli_command_source.cpp`)는 WSL/Linux 전용 의도로 legacy 그대로 억제 유지(우측 센서 무관). CWE-457 억제 항목은 현재 미재현으로 불필요해짐.

## 생성·수정 파일

- `C:\Users\윤성진\OneDrive\바탕 화면\Konkuk\3-1\객체지향개발방법론\과제\TeamPractice#6\#5_Team7_NoRight\docs\sa\cppcheck.txt` (재생성)
- `C:\Users\윤성진\OneDrive\바탕 화면\Konkuk\3-1\객체지향개발방법론\과제\TeamPractice#6\#5_Team7_NoRight\docs\sa\clang-tidy.txt` (재생성)
- `C:\Users\윤성진\OneDrive\바탕 화면\Konkuk\3-1\객체지향개발방법론\과제\TeamPractice#6\#5_Team7_NoRight\docs\sa\summary.md` (재생성, legacy 대비 비교 포함)
- (로그) `C:\Users\윤성진\OneDrive\바탕 화면\Konkuk\3-1\객체지향개발방법론\과제\TeamPractice#6\#5_Team7_NoRight\log\2026-05-29_1518_sa.md`
- **코드 수정 없음** (분석 결과 신규 이슈 0건 → 수정 불요)

## 차이 표기

legacy 리포트 대비: **프로젝트 이슈 기준 일치(0→0)**, 수치 변화 1건은 toolchain 차이.

| 항목 | legacy | 수정 | 판정 |
|---|---|---|---|
| -Wall/-Wextra/-Wpedantic/-Wconversion/-Wshadow | 0 | 0 | 일치 |
| -fanalyzer 프로젝트 이슈 | 0 | 0 | 일치 |
| -fanalyzer STL 거짓 양성 | 5 | 0 | 변경(감소) — toolchain 차이, legacy 코드도 현재 0건 |
| error/warning/style/performance | 0 | 0 | 일치 |
| portability(POSIX, 억제) | 1 | 1 | 일치 |
| 우측 센서 삭제로 인한 신규 이슈 | — | 0 | 신규 없음(멤버·인자·분기 완전 제거) |

## 결과 요약

- 분석 전/후 프로젝트 이슈: **0 → 0** (수정 불요). 수정 코드 6파일 모두 GCC 진단 clean.
- 우측 근접 센서 삭제는 정적 분석 이슈를 **0건 유발**. `ObstacleHandler`의 `right`/`right_`·우측 회피 분기·`AvoidPath::TURN_RIGHT` 케이스를 잔여 없이 제거해 미사용/도달불가 경고 없음.
- legacy 대비 유일한 수치 델타(-fanalyzer 5→0)는 toolchain 차이이며 코드 변경과 무관(legacy 코드도 현재 toolchain에서 0건).

## 검증 방법

```bash
# cppcheck 대체 (전 소스, GCC 컴파일러 진단)
wsl bash -lc "cd '/mnt/c/Users/윤성진/OneDrive/바탕 화면/Konkuk/3-1/객체지향개발방법론/과제/TeamPractice#6/#5_Team7_NoRight' && g++ -std=c++17 -Wall -Wextra -Wpedantic -Wconversion -Wshadow -Iinclude -fsyntax-only src/core/cleaning_engine.cpp src/core/obstacle_handler.cpp src/core/clean_intensity_controller.cpp src/app/console_log.cpp src/app/cli_command_source.cpp src/app/main.cpp"

# clang-tidy 대체 (core, GCC -fanalyzer)
wsl bash -lc "cd '/mnt/c/Users/윤성진/OneDrive/바탕 화면/Konkuk/3-1/객체지향개발방법론/과제/TeamPractice#6/#5_Team7_NoRight' && g++ -std=c++17 -fanalyzer -Wall -Wextra -Wpedantic -Iinclude -fsyntax-only src/core/cleaning_engine.cpp src/core/obstacle_handler.cpp src/core/clean_intensity_controller.cpp"
```

두 명령 모두 출력 없음(exit 0) = clean.

> 주: cppcheck/clang-tidy 가 설치된 환경이라면 `.claude/commands/sa.md` 의 원래 명령을 그대로 써도 된다. 본 환경은 미설치이므로 legacy와 동일한 GCC 대체 방법론을 사용했다.

## 다음 단계

`/package` — 통합 diff 매트릭스 + 클린 빌드 재현 + README/INDEX 최종화.
