# /st

## 사용 프롬프트
`/st`

## 주요 결정

- **기준선 인계**: `legacy/test/system/`의 두 파일(`test_system_scenarios.cpp`, `test_uc_scenarios.cpp`)을 최상위 `test/system/`로 인계받아 우측 근접 센서 삭제가 닿는 부분만 최소 수정. 시나리오를 새로 짜지 않음.
- **회피 시나리오 재구성**: 우측 근접 센서가 사라져 `selectPath()`가 "좌측 가능→좌회전 / 좌측 막힘→후진"으로 단순화됨에 따라(impact.md §5·SRS §3.2 결정 표):
  - legacy의 **우측 경로 시나리오 2건 삭제** — `SystemScenario.ObstacleAvoidanceRightPath`(좌 막힘·우 비면→우회전), `UC03_ObstacleAvoidance.RightPath_WhenLeftBlockedRightClear`. 우측 센서가 없으면 이 경로는 선택될 수 없으므로 시나리오 자체가 성립하지 않음.
  - **후진 시나리오 트리거 단순화** — legacy는 "전·좌·우 모두 막힘"으로 후진을 유발했으나, 이제 "전방 회피 + 좌측 막힘"만으로 후진 경로 진입. `setRightBlocked(true)` 주입 제거, `BackPath_WhenAllBlocked` → `BackPath_WhenLeftBlocked`로 개명.
- **수정 CleaningEngine ctor 정합**: `(front, left, dust, drive, cleaner, log, commands)`로 7-인자화(우측 센서 인자 제거)된 시그니처에 픽스처·오류처리 시나리오의 직접 생성 코드를 맞춤.
- **수정 하니스(RVCSimHarness) API 정합**: `setRightBlocked`/`injectRightObstacle`/`rightSensorCalls` 제거된 API에 맞춰 UC 시나리오의 우측 주입 호출 제거.
- **보존(일치)한 시나리오**: 전원 생명주기(UC-01/UC-02/UC-05), 강화 청소(UC-04), 좌회전 회피(UC-03 Path1), 전방 센서 마스킹(FR-MOVE-03), 명령 시퀀스, 오류 종료(UC-06)의 **검증 의도·기대값**은 legacy 그대로. 우측 센서 인자·주입만 정리. UC 커버리지(UC-01~06) 100% 유지.
- **우회전(모터 명령) 미삭제**: `MotorCommand::TURN_RIGHT`는 모터 출력으로 존속(SRS §6.2). ST에서는 우측 센서 기반 회피 경로 시나리오만 삭제했고, 후진 후 blind 최후 탈출(좌 센서만 사용)은 코드상 유지되므로 별도 신규 시나리오는 추가하지 않음(최소 수정).

## 생성·수정 파일

- `C:\Users\윤성진\OneDrive\바탕 화면\Konkuk\3-1\객체지향개발방법론\과제\TeamPractice#6\#5_Team7_NoRight\test\system\test_system_scenarios.cpp`
- `C:\Users\윤성진\OneDrive\바탕 화면\Konkuk\3-1\객체지향개발방법론\과제\TeamPractice#6\#5_Team7_NoRight\test\system\test_uc_scenarios.cpp`
- (로그) `C:\Users\윤성진\OneDrive\바탕 화면\Konkuk\3-1\객체지향개발방법론\과제\TeamPractice#6\#5_Team7_NoRight\log\2026-05-29_1510_st.md`

## 차이 표기

legacy 대비: **변경 2파일 / 일치(미변경 시나리오) 다수**.

| 시나리오/파일 | legacy 대비 | 내용 |
|---|---|---|
| `test_system_scenarios.cpp` 픽스처 `SystemFixture` | 변경 | `StubProximitySensor front, left, right` → `front, left`; ctor `{front,left,right,…}` → `{front,left,…}` |
| `SystemScenario.ObstacleAvoidanceRightPath` | 변경(삭제) | 우측 센서 기반 우회전 경로 — 우측 센서 제거로 성립 불가 |
| `SystemScenario.ObstacleAvoidanceBackPath` | 변경 | 트리거를 "좌·우 모두 막힘"→"좌측 막힘"으로 단순화(`right.setBlocked(true)` 제거); 기대값(BACKWARD/"후진") 동일 |
| `SystemScenario.ObstacleAvoidanceLeftPath` | 일치 | 좌회전 경로, 센서 인자만 정합 |
| `SystemScenario.InternalErrorCausesSafeHalt` | 변경 | `StubProximitySensor left, right`→`left`; ctor `(broken,left,right,…)`→`(broken,left,…)`. 검증 의도 동일 |
| `SystemScenario` 전원/강화 청소(Normal/Boost*) | 일치 | 센서 무관, legacy 그대로 |
| `test_uc_scenarios.cpp` `UC03…RightPath_WhenLeftBlockedRightClear` | 변경(삭제) | 우측 경로 시나리오, `setRightBlocked` 사용 |
| `test_uc_scenarios.cpp` `UC03…BackPath_WhenAllBlocked` | 변경 | → `BackPath_WhenLeftBlocked`; `setRightBlocked(true)` 제거, 좌측 막힘만으로 후진 검증 |
| `UC06_ErrorShutdown` 2건 | 변경 | `SimProximitySensor left, right`→`left`; ctor 우측 인자 제거. 검증 의도 동일 |
| `UC01/UC02/UC04/UC05` + `UC03`(LeftPath·복귀·센서마스킹·CommandSequence) | 일치 | 우측 센서 무관 — legacy 시나리오 그대로 (하니스 새 API 사용) |

확인 명령: `wsl bash -lc "diff -rq legacy/test/system test/system"` → 두 .cpp만 differ(+ legacy의 `.gitkeep`).

## 결과 요약

- 시스템 테스트 2파일 인계·수정. 우측 근접 센서 기반 회피 경로 시나리오 **2건 삭제**(파일별 1건씩), 후진 시나리오 트리거 단순화, 오류처리·픽스처의 우측 센서 인자 제거.
- 테스트 수: legacy 25건(7+18 등가) → 수정 후 **24건**(System 6 + UC 18). UC-01~UC-06 **전 UC 커버리지 유지**.
- 좌회전/후진/강화청소/전원/오류 등 센서 무관 시나리오는 legacy 검증 의도·기대값 그대로 보존(일치).
- 빌드 성공, **24/24 전부 통과**.

## 검증 방법

```bash
wsl bash -lc "cd '/mnt/c/Users/윤성진/OneDrive/바탕 화면/Konkuk/3-1/객체지향개발방법론/과제/TeamPractice#6/#5_Team7_NoRight' && cmake -S . -B build && cmake --build build -j --target oop_system_test && ctest --test-dir build -R 'UC0|SystemScenario' --output-on-failure"
```

> 주: 시스템 테스트는 GTest `gtest_discover_tests`로 개별 테스트명(`UC0*`, `SystemScenario.*`)으로 등록되므로 `-R system_tests`로는 매칭되지 않는다. `-R 'UC0|SystemScenario'`(또는 바이너리 직접 실행 `./build/oop_system_test`)로 실행한다.

## 다음 단계

`/sa` — 수정 코드에 cppcheck + clang-tidy 재실행, legacy 리포트 대비 이슈 증감 비교.
