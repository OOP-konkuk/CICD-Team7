# /simulator

## 사용 프롬프트
`/simulator`

## 주요 결정
- **legacy `test/simulator/`를 그대로 인계 후 `rvc_sim_harness.hpp`에서 우측 센서 배선·주입 API만 제거.** Sim 클래스 이름·하니스 구조·쿼리 API는 legacy 그대로.
- **하니스 변경점**:
  - 엔진 조립 `engine_(front_, left_, right_, dust_, …)` → `engine_(front_, left_, dust_, …)` (수정된 7+1인자 `CleaningEngine` ctor에 정합).
  - 멤버 `SimProximitySensor front_, left_, right_;` → `front_, left_;`.
  - 환경 주입 API에서 우측 경로 제거: `setRightBlocked()`, `injectRightObstacle()` 삭제.
  - 콜카운트 API `rightSensorCalls()` 삭제, `resetSensorCallCounts()`에서 `right_` 항목 제거.
  - 직접 접근자 `rightSensor()` 삭제.
- **`sim_proximity_sensor.hpp`는 일치**: 방향 비특정(`isBlocked()`/`setBlocked()`/`enqueueEvent()`) 클래스라 우측 센서 삭제와 무관 — 클래스 자체 무변경, 하니스의 인스턴스 수(3→2)만 변동.
- **나머지 Sim 5종 일치**: dust/drive/cleaning_unit/output/command 모두 센서 무관.
- **하니스 주석 "six Sim implementations" 유지**: Sim *클래스* 수는 여전히 6종(proximity·dust·drive·cleaning·output·command). 줄어든 것은 proximity 인스턴스 수이므로 문구 일치.
- **빌드 검증 방식**: 현재 `test/system/`이 비어 `oop_system_test` 타겟이 없으므로(다음 `/st`에서 생성), 하니스를 단독 TU로 컴파일+`rvc_core` 링크하여 헤더 정합성을 선검증. 새 8인자 `CleaningEngine` ctor에 정확히 링크됨.

## 생성·수정 파일
변경(1):
- `...\test\simulator\rvc_sim_harness.hpp`

## 차이 표기
`diff -rq legacy/test/simulator test/simulator` 기준:

| 파일/API | legacy 대비 | 내용 |
|---|---|---|
| `rvc_sim_harness.hpp` | 변경 | `right_` 멤버·엔진 조립 인자 제거; `setRightBlocked`·`injectRightObstacle`·`rightSensorCalls`·`rightSensor()` 삭제; `resetSensorCallCounts` 내 `right_` 제거 |
| `sim_proximity_sensor.hpp` | 일치 | 방향 비특정 Sim 클래스, legacy 그대로 |
| `sim_dust_sensor.hpp` | 일치 | 센서 무관 |
| `sim_drive_train.hpp` | 일치 | 센서 무관 |
| `sim_cleaning_unit.hpp` | 일치 | 센서 무관 |
| `sim_output_log.hpp` | 일치 | 센서 무관 |
| `sim_command_source.hpp` | 일치 | 센서 무관 |

요약: **변경 1파일 / 일치 6파일**. 제거된 환경 주입·조회 API 4개(setRightBlocked, injectRightObstacle, rightSensorCalls, rightSensor).

## 결과 요약
legacy 시뮬레이터를 인계받아 테스트 하니스(`rvc_sim_harness.hpp`)에서 우측 센서 인스턴스·배선·주입/조회 API만 제거했다. Sim 클래스 6종은 모두 일치(방향 비특정 `SimProximitySensor` 포함). 하니스를 단독 컴파일+`rvc_core` 링크로 선검증 — 새 `CleaningEngine` ctor(우측 인자 없음)에 정합하게 링크·실행 성공, 경고 0. 실제 시스템 시나리오 실행은 `/st`에서 수행.

## 검증 방법
```bash
# 하니스 헤더 정합성 선검증 (test/system 비어 oop_system_test 미생성 상태)
wsl bash -lc "cd '/mnt/c/Users/윤성진/OneDrive/바탕 화면/Konkuk/3-1/객체지향개발방법론/과제/TeamPractice#6/#5_Team7_NoRight' && printf '#include \"rvc_sim_harness.hpp\"\nint main(){ RVCSimHarness h; h.setFrontBlocked(true); h.setLeftBlocked(false); return 0; }\n' > /tmp/sim_check.cpp && g++ -std=c++17 -Wall -Wextra -Wpedantic -Iinclude -Itest/simulator -Itest/unit/stub /tmp/sim_check.cpp -L build -lrvc_core -o /tmp/sim_check2 && /tmp/sim_check2"
# legacy 대비 차이
wsl bash -lc "cd '...#5_Team7_NoRight' && diff -rq legacy/test/simulator test/simulator"
```
결과: 컴파일·링크·실행 OK(경고 0). diff — `rvc_sim_harness.hpp`만 차이. 시뮬레이터는 test 전용으로 `rvc_core`/production에 포함되지 않음. 원본 출처(PDF·CICD·objective.md) 미열람.

## 다음 단계
`/st`
