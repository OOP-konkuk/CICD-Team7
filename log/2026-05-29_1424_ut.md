# /ut

## 사용 프롬프트
`/ut`

## 주요 결정
- **legacy `test/unit/`을 그대로 인계 후 우측 센서·우측 분기 관련 케이스만 수정.** 테스트 파일명·구조는 수정 구현(=legacy 구조)을 따른다.
- **`test_obstacle_handler.cpp`**:
  - 픽스처 `StubProximitySensor front, left, right;` → `front, left;`, `handler{left, right, …}` → `handler{left, …}`.
  - **`TurnRightWhenLeftBlockedRightClear` 테스트 삭제** — "좌 막힘·우 비면 우회전"은 우측 센서 판독 경로(`AvoidPath::TURN_RIGHT`)였고 폐지됨. 삭제 사유를 주석으로 명시(추적성).
  - `BackwardThenTurnWhenAllBlocked` → `BackwardThenTurnWhenLeftBlocked`로 개명·조건 단순화(`right.setBlocked` 제거). 후진 후 좌 막힘 → `TURN_RIGHT`(blind) 기대값은 **유지**(REVIEW 권장안: executeBackAndTurn 존속).
  - `BackwardThenLeftTurnIfLeftClearsAfterReverse`: `right.enqueueResult(true)` 제거(selectPath는 이제 좌만 검사). 좌 enqueue(true→false)로 후진 후 좌 클리어 시 좌회전 검증.
  - `NoSensorPollingDuringRotation`: `right` 리셋·`right.callCount()==0` 단언 제거, 주석을 "좌만 폴링"으로. `front.callCount()==0`·`left.callCount()==1` 유지.
  - `CommandOrder_BackPath_*`: `right.setBlocked(true)` 제거. BACKWARD→…→TURN_RIGHT(blind)→FORWARD 순서 검증 유지.
- **`test_cleaning_engine.cpp`**: 픽스처 `front, left, right;`→`front, left;`, `makeEngine()`·`ExceptionCausesSafeHalt`의 `CleaningEngine(…, right, …)`에서 `right` 인자 제거. 센서 동작 케이스는 front/left/dust 기반이라 로직 변경 없음.
- **스텁·`test_clean_intensity_controller.cpp`는 일치**: `stub_proximity_sensor.hpp`는 방향 비특정이라 무변경. 나머지 5개 스텁·강도 컨트롤러 테스트는 센서 무관.
- **`TURN_RIGHT` 기대값 유지 근거**: 후진 후 blind 우회전(`MotorCommand::TURN_RIGHT`)은 모터 명령으로 존속하므로 BackPath 테스트의 TURN_RIGHT 단언을 그대로 둔다.

## 생성·수정 파일
변경(2):
- `...\test\unit\test_obstacle_handler.cpp`
- `...\test\unit\test_cleaning_engine.cpp`

## 차이 표기
`diff -rq legacy/test/unit test/unit` 기준:

| 파일/케이스 | legacy 대비 | 내용 |
|---|---|---|
| `test_obstacle_handler.cpp` | 변경 | 픽스처 right 제거, `TurnRightWhenLeftBlockedRightClear` 삭제, BackPath 케이스 조건 단순화(개명), NoSensorPolling/enqueue/CommandOrder의 right 참조 제거 |
| `test_cleaning_engine.cpp` | 변경 | 픽스처·엔진 ctor 2곳에서 right 인자 제거 |
| `stub/stub_proximity_sensor.hpp` | 일치 | 방향 비특정 클래스, legacy 그대로 |
| `stub/stub_{dust_sensor,drive_train,cleaning_unit,output_log,command_source}.hpp` | 일치 | 센서 무관 |
| `test_clean_intensity_controller.cpp` | 일치 | 센서 무관 |

요약: **변경 2파일 / 일치 7파일**.

## 결과 요약
legacy 단위 테스트를 인계받아 우측 센서 픽스처·인자와 우측 분기 검증을 일관 정리했다. 우측 센서 전용 케이스 1개(`TurnRightWhenLeftBlockedRightClear`)를 삭제(사유 주석)하고, 후진-후-blind-우회전 경로 케이스는 기대값 유지. 테스트 수 30→**29**(삭제 1). **29/29 통과(100%)**, 빌드 경고 0. 스텁 6종·강도 컨트롤러 테스트는 일치.

## 검증 방법
```bash
wsl bash -lc "cd '/mnt/c/Users/윤성진/OneDrive/바탕 화면/Konkuk/3-1/객체지향개발방법론/과제/TeamPractice#6/#5_Team7_NoRight' && cmake -S . -B build && cmake --build build -j --target oop_test && ctest --test-dir build --output-on-failure"
wsl bash -lc "cd '...#5_Team7_NoRight' && diff -rq legacy/test/unit test/unit"
```
결과: 29/29 통과, EXIT 0. diff — test_obstacle_handler.cpp·test_cleaning_engine.cpp만 차이.
원본 출처(PDF·CICD·objective.md)·docs/specs 미열람. 스텁은 test 전용으로 production(`src/`,`include/`)에 섞이지 않음.

## 다음 단계
`/simulator`
