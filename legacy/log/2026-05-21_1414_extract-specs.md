# /extract-specs

## 사용 프롬프트
`/extract-specs`

## 주요 결정

### 1. 통합: 장애물 회피 UC4·UC5·UC6 → UC-03 단일 UC
원본의 UC3(감지 허브)·UC4(좌회전)·UC5(우회전)·UC6(후진+회전)은 동일 트리거(전방 장애물)에서 파생되는 세 분기 경로로, 독립 트리거가 없다. 이를 UC-03 하나로 통합하고 방향 결정 로직을 표로 표현하였다. 테스트 시나리오 작성 시 3개 경로를 표의 각 행으로 직접 대응시킬 수 있어 검증 가능성이 향상된다.

### 2. 변환: 타이밍 NFR → FR 검증 기준으로 이전
원본 NFR-P-01~P-06은 독립 항목으로 나열되어 있으나, 각 수치는 대응하는 기능 요구사항의 검증 기준으로 표현하는 것이 더 명료하다. NFR-TIMING-* 테이블을 별도로 유지하되 FR-CTRL-01, FR-MOVE-02, FR-CLEAN-02에 검증 기준으로도 부속시켰다.

### 3. 명시화: 입력 마스크 규칙을 별도 FR로 격상
원본 NFR-O-06(회전 중 센서 무시), NFR-O-07(강화 모드 중 먼지 무시), NFR-O-08(강화 모드 중 장애물 회피 허용)은 관련 FR(FR-MOVE-03, FR-CLEAN-02)의 부속 규칙으로 직접 명시화하였다. 별도 NFR로 분리하면 FR과 NFR 사이에 이중 기술이 생기고 추적성이 낮아진다.

### 4. 변환: NFR-O-09 → NFR-SAFETY-01로 격상
원본 NFR-O-09(장애물 > 먼지 우선순위)는 "Other requirements"에 묻혀 있었으나, 이 규칙은 이벤트 처리 순서를 결정하는 핵심 안전 정책이다. NFR-SAFETY-01로 격상하고 FR-SENSE-01에도 우선순위 언급을 추가하였다.

### 5. 삭제: NFR-O-05 (인증 정보 하드코딩 금지)
해당 요구사항은 CI/CD 환경의 AWS/GitHub Secrets 관리 정책으로, TP#5(로컬 WSL 빌드)에는 적용 대상이 없다. reference/ 정보에 따르면 이전 학기 팀의 CI/CD 인프라에 특화된 항목으로 판단하여 제외하였다.

### 6. 제외: PDF Domain Model·Class Diagram·Sequence Diagram
CLAUDE.md 2절·6절에 따라 PDF의 클래스 이름(RVCOrchestrator, CLIHandler 등), 메서드 시그니처(startCleaning(), requestStatus() 등), CICD-Team7-main 디렉토리 구조를 산출물에 일절 반영하지 않았다. 모든 FR/UC/NFR은 시스템의 의도(외부 자극과 기대 동작)만을 재진술한 것이다.

---

## 생성·수정 파일
- `/mnt/c/Users/윤성진/OneDrive/바탕 화면/Konkuk/3-1/객체지향개발방법론/과제/TeamPractice#5/dev/#5_Team7/docs/specs/system-overview.md`
- `/mnt/c/Users/윤성진/OneDrive/바탕 화면/Konkuk/3-1/객체지향개발방법론/과제/TeamPractice#5/dev/#5_Team7/docs/specs/requirements.md`
- `/mnt/c/Users/윤성진/OneDrive/바탕 화면/Konkuk/3-1/객체지향개발방법론/과제/TeamPractice#5/dev/#5_Team7/docs/specs/use-cases.md`

---

## 결과 요약

| 항목 | 원본 | 재증류 | 변화 |
|------|------|--------|------|
| 기능 요구사항 (FR) | R1.1~R6.1 (12개) | FR-CTRL-01~03, FR-MOVE-01~03, FR-CLEAN-01~02, FR-SENSE-01~02 (10개) | 통합 2건, 타이밍 기준은 NFR로 이전 |
| 비기능 요구사항 (NFR) | NFR-P(6) + NFR-O(9) + NFR-OE(1) + NFR-I(2) = 18개 | NFR-ARCH(3) + NFR-TIMING(8항목) + NFR-BUILD(1) + NFR-UI(2) + NFR-SAFETY(1) = 15개 | 통합 2건, 삭제 1건(NFR-O-05), 격상 1건 |
| 유스케이스 (UC) | 9개 | 6개 | UC3~6 → UC-03 통합 |
| 보존된 강제 조항 | - | C++17/CMake3.14(NFR-BUILD-01), 추상화·Stub(NFR-ARCH-01~02), 시간 수치 전체(NFR-TIMING-01~06+04a~c), 장애물 우선(NFR-SAFETY-01), 회전 입력 마스크(FR-MOVE-03), 강화 모드 입력 마스크(FR-CLEAN-02) | 모두 보존 |

**자기 점검 결과**:
- [x] PDF 표·문장을 한 줄도 그대로 복사하지 않았다
- [x] 강제 조항(C++17/CMake, 시간 한계, 추상화/Stub, 우선순위 규칙) 전체 추적 가능
- [x] 추적성 표로 원본 ID ↔ 새 ID 매핑 완전히 기재
- [x] PDF의 클래스명·메서드명·CICD-Team7 디렉토리명이 산출물에 등장하지 않음
- [x] 새 UC 흐름이 원본보다 짧고 검증 시그널이 명확

---

## 검증 방법

```bash
wsl bash -lc "ls docs/specs && wc -l docs/specs/*.md && grep -c '^####' docs/specs/requirements.md"
```

---

## 다음 단계
`/srs` — `docs/specs/`의 재증류 결과만 입력으로 사용하여 SRS를 작성한다.
